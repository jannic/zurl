zurl for Debian
---------------

As there is no released tarball of zurl, yet, the orig tarball was created
from the upstream git at git://github.com/fanout/zurl.git

This also includes the git submodules src/common and src/qzmq.

